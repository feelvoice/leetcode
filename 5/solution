最长回文子串，线性算法：Manacher算法 - -

略犀利~

字符串以#分隔重组，不再判定奇偶。前后增加一个sentinel(^$)简化边界条件
问题转化为遍历该字符串，求以当前下标为中心，回文最长的子串长度P[i]，最终解为max(P[i])
求解时维护当前已遍历的子串里最长子串的中心C和对应的右边界R。
如在特定范围，P[i]可直接由镜像获得，否则则需要遍历计算。根据新计算的P[i]调整C,R即可。

略trick, 知道算法即可。
